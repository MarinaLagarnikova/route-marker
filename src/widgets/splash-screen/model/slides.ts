export interface SplashSlide {
  image: string
  caption: string
}

/** Заголовок остаётся на месте, пока слайды сменяют друг друга. */
export const SPLASH_TITLE = 'Еще чуть чуть — и все загрузим'

/**
 * Порядок важен: при быстрой связи загрузка успевает закончиться на первом слайде,
 * поэтому дальше первого большинство людей ничего не увидит. Первым идёт тот,
 * что лучше всего представляет приложение.
 */
export const SPLASH_SLIDES: SplashSlide[] = [
  { image: '/splash/slide-2.jpg', caption: 'Активный отдых — лучший способ восстановиться' },
  { image: '/splash/slide-1.jpg', caption: 'Даже в мегаполисе есть куда пойти пешком' },
  { image: '/splash/slide-3.jpg', caption: 'Выбери время, чтобы остановиться и посмотреть' },
]

/** Сколько держится один слайд. Хватает, чтобы прочитать подпись без спешки. */
export const SLIDE_DURATION_MS = 4200

/** Длительность переезда между слайдами; совпадает с CSS-транзишеном. */
export const SLIDE_TRANSITION_MS = 600

/**
 * Минимум, сколько экран висит, даже если данные пришли мгновенно, — иначе на
 * быстрой связи он мигнёт и собьёт с толку. Хватает, чтобы прочитать первую подпись.
 */
export const MIN_DISPLAY_MS = 2400

/**
 * Потолок ожидания. Если связи нет или она очень медленная, экран всё равно
 * уходит: приложение рассчитано на работу офлайн, держать человека нельзя.
 * Подобран так, чтобы успел начаться третий слайд.
 */
export const MAX_DISPLAY_MS = 12000

/** Ключ отметки о показе: экран приветствия показывается один раз. */
export const SPLASH_SEEN_KEY = 'splash_seen'
